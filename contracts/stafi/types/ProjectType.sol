pragma solidity 0.8.19;

// SPDX-License-Identifier: GPL-3.0-only

struct Project {
    uint256 pId;
    address balances;
    address distributor;
    address feePool;
    address lightNode;
    address nodeManager;
    address rToken;
    address settings;
    address superNode;
    address projEther;
    address userDeposit;
    address withdraw;
}
